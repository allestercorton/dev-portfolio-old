RewriteEngine On

# Hide index.html from the URL
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s(.*)/index\.html [NC]
RewriteRule ^ %1 [R=301,L]

# Prevent direct access to style.css and script.js
RewriteCond %{THE_REQUEST} \s/(style|script)\.css [NC]
RewriteRule ^ - [F,L]